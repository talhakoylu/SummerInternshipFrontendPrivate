import React from 'react'
import Modal from './../../Modal';

export default function RegisterModal({ ...dist }) {

    return (
        <Modal title={"Kayıt Ol"} {...dist}>
           İçerik
        </Modal>
    );
}
